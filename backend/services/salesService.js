/**
 * Processes and saves sales report data for a store
 * @param {string} storeId - UUID from the stores table
 * @param {Array} orders - Array of orders from Shopify API
 */
async function saveSalesFromOrders(storeId, orders) {
  // TO DO: parse orders, insert per line_item into sales_reports
}
